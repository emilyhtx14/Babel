//////////. and how to format 
//how to create multiple buttons //initially sets answer to hiddenconst wordList = doucmentscuments.getElementByClassName()""worlist[]0;

/*
oconst wfrotn nt - = documents.getElementByClassName("worlist")[0];thefront
const front = documents.getElementByClassName("thefront")[0];
backtehheback
*/

$(document).ready(function() {
    $('.hover').on('touchstart touchend', function(e) {
        e.preventDefault();
        $(this).toggleClass('hover_effect');
    });
});